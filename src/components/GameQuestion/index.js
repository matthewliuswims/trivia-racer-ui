import React from "react"

// Styled
import { StyledGameQuestion } from "./styled"

const GameQuestion = ({ question }) => {
  return <StyledGameQuestion>{question}</StyledGameQuestion>
}

export default GameQuestion
